name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # ogni 5 minuti

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl -s https://fx-api-vpxj.onrender.com/ > /dev/null
